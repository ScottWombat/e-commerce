import styles from './forget-password.module.css';
import * as Constants from 'src/constants';
const ForgetPassword = () => {
    return(
        <section>
        <div>{Constants.API_BASE_URL} kkkkk</div>
        </section>
    );
};

export default ForgetPassword;
